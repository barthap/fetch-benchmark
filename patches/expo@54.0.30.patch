diff --git a/android/src/main/java/expo/modules/fetch/ExpoFetchModule.kt b/android/src/main/java/expo/modules/fetch/ExpoFetchModule.kt
index 7c23ce4ac4ad4e341f305f40f68046cdebf5c7a1..e9d204688290e6a9a7cdae996f9122e608d167ec 100644
--- a/android/src/main/java/expo/modules/fetch/ExpoFetchModule.kt
+++ b/android/src/main/java/expo/modules/fetch/ExpoFetchModule.kt
@@ -11,6 +11,7 @@ import expo.modules.core.errors.ModuleDestroyedException
 import expo.modules.kotlin.Promise
 import expo.modules.kotlin.exception.Exceptions
 import expo.modules.kotlin.exception.toCodedException
+import expo.modules.kotlin.jni.NativeArrayBuffer
 import expo.modules.kotlin.modules.Module
 import expo.modules.kotlin.modules.ModuleDefinition
 import kotlinx.coroutines.CoroutineName
@@ -101,6 +102,13 @@ class ExpoFetchModule : Module() {
           promise.resolve(data)
         }
       }
+      
+      AsyncFunction("arrayBuffer2") { response: NativeResponse, promise: Promise ->
+        response.waitForStates(listOf(ResponseState.BODY_COMPLETED)) {
+          val data = response.sink.finalizeDirect()
+          promise.resolve(NativeArrayBuffer(data))
+        }
+      }
 
       AsyncFunction("text") { response: NativeResponse, promise: Promise ->
         response.waitForStates(listOf(ResponseState.BODY_COMPLETED)) {
diff --git a/android/src/main/java/expo/modules/fetch/ResponseSink.kt b/android/src/main/java/expo/modules/fetch/ResponseSink.kt
index 3a8b26b865b94a3e69c90aa709b636fde0efa3fd..5c043488444541445ed88c26a1af8d9e7647ef83 100644
--- a/android/src/main/java/expo/modules/fetch/ResponseSink.kt
+++ b/android/src/main/java/expo/modules/fetch/ResponseSink.kt
@@ -10,6 +10,7 @@ internal class ResponseSink {
   var bodyUsed = false
     private set
 
+
   internal fun appendBufferBody(data: ByteArray) {
     bodyUsed = true
     bodyQueue.add(data)
@@ -26,4 +27,16 @@ internal class ResponseSink {
     isFinalized = true
     return byteBuffer.array()
   }
+
+  fun finalizeDirect(): ByteBuffer {
+    val size = bodyQueue.sumOf { it.size }
+    val byteBuffer = ByteBuffer.allocateDirect(size)
+    for (byteArray in bodyQueue) {
+      byteBuffer.put(byteArray)
+    }
+    bodyQueue.clear()
+    bodyUsed = true
+    isFinalized = true
+    return byteBuffer
+  }
 }
diff --git a/ios/Fetch/ExpoFetchModule.swift b/ios/Fetch/ExpoFetchModule.swift
index 75c2adc43b4ad8d6026de06edd397e70d6a2a1eb..28d4a6b09c42ff20d2d25f0cd7a8fea651f30013 100644
--- a/ios/Fetch/ExpoFetchModule.swift
+++ b/ios/Fetch/ExpoFetchModule.swift
@@ -61,6 +61,13 @@ private let fetchRequestQueue = DispatchQueue(label: "expo.modules.fetch.Request
           promise.resolve(data)
         }
       }.runOnQueue(fetchRequestQueue)
+      
+      AsyncFunction("arrayBuffer2") { (response: NativeResponse, promise: Promise) in
+        response.waitFor(states: [.bodyCompleted]) { _ in
+          let data = response.sink.finalize()
+          promise.resolve(ArrayBuffer.wrap(dataWithoutCopy: data))
+        }
+      }.runOnQueue(fetchRequestQueue)
 
       AsyncFunction("text") { (response: NativeResponse, promise: Promise) in
         response.waitFor(states: [.bodyCompleted]) { _ in
